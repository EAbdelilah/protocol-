import '@testing-library/jest-dom';

// You can add other global setup configurations here if needed
// For example, mocking global objects:
// global.ResizeObserver = require('resize-observer-polyfill')

// If you need to mock a specific module globally for all tests:
// vi.mock('some-module', () => ({
//   default: vi.fn(),
//   someNamedExport: vi.fn(),
// }));

// Clean up after each test (optional, Vitest does some auto-cleanup)
// import { afterEach } from 'vitest';
// import { cleanup } from '@testing-library/react';
// afterEach(() => {
//   cleanup();
// });
